version: '3.6'

services:

  users:
    build:
      context: https://github.com/opcodio/codecheck-users.git
      dockerfile: Dockerfile-prod
    environment:
      - FLASK_ENV=production
      - APP_SETTINGS=project.config.TestingConfig
      - DATABASE_URL=postgres://postgres:postgres@users-db:5432/users_prod
      - DATABASE_TEST_URL=postgres://postgres:postgres@users-db:5432/users_test
    depends_on:
      - users-db
    expose:
      - '5000'
  
  users-db:
    build:
      context: https://github.com/opcodio/codecheck-users.git#master:project/db
      dockerfile: Dockerfile
    ports:
      - 5435:5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
  
  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile-prod
    restart: always
    ports:
      - 80:80
    depends_on:
      - users
      - web

  web:
    build:
      context: https://github.com/opcodio/codecheck-web.git
      dockerfile: Dockerfile-dev
    volumes:
      - '../codecheck-web:/usr/src/app'
      - '/usr/src/app/node_modules'
    ports:
      - 3007:3000
    environment:
      - NODE_ENV=development
      - REACT_APP_USERS_SERVICE_URL=${REACT_APP_USERS_SERVICE_URL}
    depends_on:
      - users